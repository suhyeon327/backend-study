# 조건문

## IF

```sql
IF(조건, 조건이 참일 때, 조건이 거짓일 때)
```

## NULL 처리용 IF

```sql
IF(col IS NULL, '대체값', col)
```

## IFNULL

```sql
IFNULL(컬럼, 대체값)
```

- NULL이면 대체값, 아니면 원래값

## CASE

```sql
CASE
    WHEN 조건 THEN 값
    ELSE 값
END
```
